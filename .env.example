# Base folder containing subfolders of XML files to process
BASE_FOLDER=data/test

# ENS syntax definition file (pattern file for regex matching)
ENS_SYNTAX_FILE=data/ENS_Syntax.txt

# Excel files for tag mapping and reference data
TAGS_PATH=data/Tags.xlsx
DOCS_PATH=data/Docs.xlsx
DOC_TAG_PATH=data/Doc-Tag.xlsx

# Optional: Output log file (if custom path is desired)
# LOG_FILE=logs/error_log.txt

# Optional: OmniPage settings file (if used in other scripts)
# SETTINGS_FILE=data/settings_new.sts
